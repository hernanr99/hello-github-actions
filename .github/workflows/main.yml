on:
  workflow_dispatch:
  

jobs:
  build:
    name: Run API
    runs-on: ubuntu-latest
    steps:
      - name: Build API
        run: |
          echo "Build API Cos coso coso es esteee"
          sleep 10s
    outputs:
        value: "juan cruz"
