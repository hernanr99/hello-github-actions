name: Deploy
on:
  workflow_dispatch:
  push:

jobs:
  build:
    name: Run API
    runs-on: ubuntu-latest
    steps:
      - name: Build API
        run: |
          echo "Build API Cos coso coso es esteee"
          sleep 10s
